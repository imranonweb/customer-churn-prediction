# =============================================================
# Customer Churn Prediction + XAI — Python dependencies
# Tested with Python 3.14 on Windows; deployed on Streamlit
# Community Cloud (Linux).  All version pins reflect the
# exact build that produced the committed model artifacts.
# =============================================================

# Core scientific stack
numpy>=2.0
pandas>=2.2
scipy>=1.13          # required by shap and scikit-learn internals

# Machine learning
scikit-learn>=1.5
xgboost>=2.1
imbalanced-learn>=0.12

# Explainability
shap>=0.46

# Visualisation (used by pre-generated Stage 4/5 figures)
matplotlib>=3.9
seaborn>=0.13

# Model persistence
joblib>=1.4

# Streamlit application
streamlit>=1.37

# Tests
pytest>=8.3

# Development / notebooks (not needed for Streamlit deployment,
# but kept so contributors can reproduce the full pipeline locally)
jupyter>=1.1
ipykernel>=6.29
